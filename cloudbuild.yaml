steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: [ 'build', '-t', 'gcr.io/$PROJECT_ID/setsisaw-api:$_SIS_VERSION', '.' ]
substitutions:
  _SIS_VERSION: $(echo {$TAG_NAME:-build}) # default value
images:
  - 'gcr.io/$PROJECT_ID/setsisaw-api'